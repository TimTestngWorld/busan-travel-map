## Beta 2.12.0 — 2026-09-09
- 2.12 第一階段正式開始：線上景點搜尋由固定最多 20 筆，改為「先顯示 20 筆，再載入到 40／60 筆」。
- 初次搜尋只把前 20 筆放到清單與地圖，結果超過 20 筆時底部出現「再顯示 20 筆」；每次加載後清單與地圖編號同步擴充。
- 地圖只繪製目前已顯示的批次，避免一次出現 40～60 個標記把畫面塞滿；實景照片仍維持 lazy load、最多 2 個並行請求。
- Kakao 主查詢加入分頁收集，連鎖店搜尋可取得最多 60 筆候選；別名查詢仍限制單頁，控制額外搜尋請求量。
- 「附近」改為針對完整搜尋池排序，不只排序目前畫面上的前 20 筆；排序後依舊以 20 筆為一批顯示並重新編號。
- 新搜尋、清除搜尋、切換收藏時會清空舊的待載照片佇列，避免前一輪搜尋在背景繼續浪費照片 API。
- 沿用 Beta 2.11.22 已部署的 Supabase `place-photos`，本版只需更新 GitHub Pages。

## Beta 2.11.23 — 2026-09-09
- 完成 2.11 系列封版前全域審視；搜尋照片、3 張相簿、編號地圖、附近排序、桌機導覽、手機版與支付拆分均保留。
- 修正線上搜尋完成後，右下搜尋狀態仍停在「搜尋中…」的狀態文案；現在會正確顯示「找到 N 筆搜尋結果」。
- 修正線上搜尋期間地圖上方仍顯示「全釜山景點 0 個符合條件」的誤導摘要；現在改顯示「搜尋結果｜N 筆｜關鍵字」。
- 搜尋清除、切回收藏或回到一般景點模式時，地圖摘要會自動恢復一般景點計數。
- 清理 FINAL 包內殘留的 2.11.21 舊 README／UX 文件，避免日後維運誤讀。
- Supabase `place-photos` 完全沿用 2.11.22 已部署版本，本版只需更新 GitHub Pages。

## Beta 2.11.22 — 2026-09-09
- 景點線上搜尋結果加入「實景辨識」縮圖：每筆結果最多只取 1 張 Google Places 實景照，讓不熟韓文店名時可先用店面／環境判斷地點。
- 搜尋照片採 lazy load：只有進入可視範圍附近的卡片才請求照片，並限制同時最多 2 個請求、同一輪搜尋以記憶體快取重用，避免一次搜尋 10～20 間就瞬間打滿照片 API。
- 地圖預覽卡也會共用同一張實景照片；點照片可放大並保留 Google Maps／作者 attribution。
- 我的景點／既有景點詳情照片由 2 張提升為最多 3 張；三張時採 1 大＋2 小版型，手機也維持在單一畫面內。
- Google 搜尋結果若已有 Place ID，收藏後會把 Place ID 一併帶進我的景點與快取，下次開啟照片不用再重新文字比對。
- 因照片上限由 2 提升為 3，Supabase `place-photos` Edge Function 需要重新部署本版；Google API Key 仍只留在 Supabase Secret，不進前端。

## Beta 2.11.21 — 2026-09-09
- 搜尋結果地圖標記改成編號 1～20，並在搜尋清單加入相同編號；桌機標記另顯示小型名稱標籤，地圖／清單可雙向連動。
- 「附近」排序後會同步重新編號，最近結果成為 1，避免距離排序後地圖與列表序號錯置。
- 桌機版取消「更多」，住宿／出國／換錢與儲值／刷卡與支付／分帳／分享／新增／主題／教學全部直接放到單列工具列；窄桌機以水平捲動保留所有入口。
- 修正 FNC 外觀主題只改少數按鈕的問題：首頁背景、今日行程主卡、工具 icon、天氣卡、導覽與互動邊框全面吃主題色。
- 原「支付・加值」重新命名為「換錢與儲值」，專注換錢所、T-money、WOWPASS。
- 把信用卡／LINE Pay／TWQR／悠遊付指南自「出國」拆成獨立「刷卡與支付」入口；正常出國頁隱藏刷卡 tab。
- 首頁旅行工具增為 6 個，補上刷卡與支付、分享旅行，手機維持 2 欄。
- 延續 2.11.20 的手機版排版、住宿滿版、官方資訊字級與 2026/09/09 機場資料；Supabase 後端不變。

## Beta 2.11.20 — 2026-09-09
- 線上景點搜尋結果與地圖同步：清單幾筆即畫幾個紅色圓點（目前清單最多 20 筆），點圓點會高亮／捲到對應結果並開預覽。
- 搜尋完成自動 fit bounds；手機會避開底部搜尋面板，保留可視地圖區域。
- 「附近」正式定義為定位＋距離排序；線上搜尋結果也會由近到遠重排，手機景點快捷列補上「附近」。
- 修正手機首頁半欄／孤立卡片問題，主操作改穩定單欄，統計與次要工具維持 2 欄。
- 修正住宿頁兩飯店快捷超出視窗；手機機場／住宿資訊改真正單欄滿版，SVG 交通圖不再強制最小寬度，保留雙指縮放。
- 放大手機官方資訊／機場頁字級與觸控按鈕。
- 2026/09/09 重新核對 KAC／釜山市官方資料：更新出入境、海關、ZimCarry、Lost & Found 等聯絡資訊；機場巴士改採「預約或交通卡／信用卡，現金不收」與 Gate 4 附近售票機說明。
- 快速教學由 5 段概略提示重寫為 6 段逐步操作流程，補齊搜尋圓點、附近排序、行程編輯、住宿／官方資訊、分帳／分享／備份。
- Supabase 後端沿用 2.10.7，不需重新部署。

## Beta 2.11.18 — 2026-09-08
- 補回首頁可見的「快速教學」入口，避免第一次使用者必須先知道「更多」的位置。
- 首頁標題右側新增釜山 16 個行政區代表位置的即時天氣。
- 天氣採 4 區／2 區翻牌輪播＋跑馬燈，顯示目前溫度、天氣、今日高低溫與降雨機率。
- 天氣資料每 15 分鐘更新並保留最近一次成功快取；提供手動更新、Open-Meteo 資料來源與韓國氣象廳官方預報入口。
- 延續 2.11.17 全部信用卡封頂細項與 2.11.16 手機搜尋鍵盤修正。

# Beta 2.11.18 — 2026-09-08
- 九張信用卡全面補上「高回饋刷到哪裡／封頂後」：uniopen 約 NT$6,250／月、富邦 J 約 NT$33,333／季、eco 約 NT$15,000／帳單週期。
- DAWAY 多層韓國 LINE Pay 回饋拆開計算：吃喝 26.5% 完整疊加約前 NT$2,000／月；旅遊購物 9.5% 完整疊加約前 NT$5,000／月；DAWAY GO 約可延伸至 NT$20,000／帳單週期。
- 更正並補完整永豐幣倍：基本國外 2% 無上限；完成任務後海外實體最高 6%，約 NT$20,000／帳單週期吃滿 4% 加碼；新申辦 Apple Pay／Google Pay 若條件同時成立，約前 NT$10,000 可到 8%。
- 玉山 Unicard 補上韓國／日本累積加碼級距與 3,000 點上限：1.5% 級距下約 NT$200,000 可吃滿活動加碼；保留 Mastercard 韓國特店門檻。
- 聯邦吉鶴補上精確門檻：先滿 NT$15,000，再對超額部分加 3%，約總消費 NT$48,334 可吃滿每月 NT$1,000 加碼；不是整筆消費都 4%。
- Richart／CUBE 以「現行官方頁未列單一加碼金額封頂」呈現，不自行推導不存在的上限。
- 悠遊付韓國 10.5% 回饋金每階段上限 NT$600，換算約 NT$5,714 合格消費吃滿單階段回饋金。
- 重新核對 K-ETA、Q-CODE、金海機場巴士、行動電源與退稅資訊；修正舊版退稅官方連結。
- 延續 2.11.16 手機搜尋鍵盤修正，並再提高景點次資訊、地圖操作、支付與行動版小字最低可讀尺寸。
- 執行 2.11 最終 Gate：版本、85 筆景點、DOM、JS、搜尋／收藏、More、Day、住宿、支付、旅行資訊、部署包與 Supabase hash 全域重檢。

# Beta 2.11.16 — 2026-09-08
- 修正手機點景點搜尋欄時，鍵盤彈出後立刻收起、輸入框失去焦點的問題。
- 根因為鍵盤改變 viewport 高度觸發 resize，舊 responsive mount 重插搜尋 DOM；新版只在跨越手機／桌機 breakpoint 時重掛。
- 搜尋欄加入行動端 16px 最小字級與 enterkeyhint=search。

# Beta 2.11.15 — 2026-09-08
- 進行全域一致性掃描，移除正式 UI 中殘留的開發／後端技術文案與舊入口說明。
- 原始手機導覽 HTML 直接改為首頁／景點／行程／更多四鍵，避免慢載入時閃出舊版五鍵。
- 清理 More 舊定位規則與部署包歷代冗餘檔案。

# Beta 2.11.14 — 2026-09-08

- 修正桌機 More dropdown 因 legacy `top:auto!important` 導致跑位／超出視窗的問題。
- More dropdown 改為相對觸發按鈕的 fixed popover，會依視窗自動夾限位置。
- 桌機 Day rail 預設收斂空白尾端 Day，保留按鈕展開全部 14 天。
- 不變更 85 景點與 Supabase 後端。


## Beta 2.11.13 — 首頁第二層工具重新取得合理視覺權重
- 桌機版首頁「收藏景點／出國資訊／支付・加值／住宿」不再縮成小型膠囊按鈕。
- 四個入口改為等寬大型次要卡片，加入圖示、標題與一句用途說明。
- 桌機 4 欄、手機 2 欄；保留今日行程為主焦點，但不再把常用旅行工具降到幾乎看不見。
- 延續 2.11.12 的單一桌機導覽列，不恢復大面積藍色 Header。


## Beta 2.11.12 — 桌機單區導覽
- 桌機頂部取消大面積藍色導覽帶，改為白色緊湊單列。
- 主導覽與住宿／出國／支付／分帳／更多合併在同一操作區，不再分成三段。
- 統一桌機按鈕尺寸與視覺層級；手機版維持原操作。
# Beta 2.11.10 — 2026-09-08
- 重寫 App 內使用教學，以一般旅客的實際操作流程為主。
- 教學由 8 大段縮為 5 個情境：找景點、排行程、旅行當天、支付加值、其他功能。
- 修正舊版過時名稱與入口，避免教學和實際畫面不一致。
- 新增極簡同行者手冊 `USER_GUIDE_Beta2_11_10.md`。

# Beta 2.11.9 — 2026-09-08

- Marianne Hotel 機場巴士首選下車站修正為「海雲台溫泉十字路口（해운대온천사거리）」；步行約 4 分鐘／240m。
- KAC Airport Limousine 1 資訊重新核對：₩9,500、僅信用卡、07:10–21:40；英文頁目前為國際線 1F Platform 3。
- 金海機場換錢新增 MONEY BOX 金海機場店固定地圖點與官方匯率連結；位於輕軌機場站 1F。
- T-money 地圖改為附近加值提示，不再暗示是完整點位；擴充便利商店附近搜尋。
- WOWPASS 明確改為可驗證點＋官方機台地圖，不再依 Kakao 結果推定完整機台數。
- 支付・加值新增「搜尋我附近」，放大視窗字級與操作尺寸。
- 住宿頁加入抵達後 3 步驟，降低第一次抵達時的決策負擔。

# Beta 2.11.8 — 2026-09-08

- 景點頁重新分工為「搜尋景點 / 我的收藏」；線上搜尋啟動後不再把既有景點清單硬塞在搜尋結果下方。
- 搜尋結果新增「查看我的收藏」與「回到目前景點」快捷，手機搜尋時自動展開底部面板。
- 桌機利用頂部空白新增住宿、出國、支付、分帳快捷；更多選單精簡並放大字級。
- 景點、搜尋結果、更多選單、住宿/機場內容全面提升字級與觸控尺寸。
- 出國資訊恢復「住宿」頁籤，Marianne Hotel 與黃老師海景民宿均可直接查看，保留機場巴士 / 地鐵 / Naver 路線。
- 手機篩選面板進一步限制高度，避免遮蔽整頁。

# Beta 2.11.7 — 2026-09-08

- 韓國信用卡指南改為公開參考，不假設所有旅客持有相同信用卡。
- 九張卡全部提升到主畫面速查：Unicard / CUBE / Richart / J / uniopen / DAWAY / 幣倍 / 吉鶴 / eco。
- 移除「uniopen 先刷 → eco 後段」作為唯一預設方案，改為依回饋、登錄、上限、特店與支付方式選卡。
- 店家支付表的信用卡欄改為引用九張卡速查，特殊通路另標 Mastercard／銀行特案。
- 支付設定檢查補齊九張卡。
- 景點維持 85 筆；Supabase 後端未變更。

# Beta 2.11.6 — 2026-09-08

- 補完整 2026/9–12 悠遊付韓國活動：12% 回饋、分階段上限、可疊加活動、遊韓雙重禮、實體悠遊卡 × 樂天免稅店。
- 新增 TWQR / Paybooc / LINE Pay 差異說明：TWQR 在韓國接入 BC Card QR 網路，但 LINE Pay 為獨立受理與活動名單，不能看到 TWQR 就推定可用 LINE Pay。
- 韓國常見通路改為三種支付路徑：LINE Pay、信用卡、悠遊付；針對樂天免稅店、LOTTE Mart、樂天百貨、CU、7-ELEVEN、GS25、OLIVE YOUNG、DAISO 等整理。
- 保留 85 筆景點與 Beta 2.11.4 HAR 第四版更新內容；Supabase 後端不變。

# Beta 2.11.5 — 2026-09-08

- 全站使用者文案重新審視：移除「你有的卡／你的住宿／這趟可不帶」等只對單一使用者說話的語氣，改成適合公開分享的中性旅遊指南文案。
- 行動支付主區只保留 LINE Pay 與悠遊付 Easy Wallet。
- LINE Pay 明確標示：持有永豐 DAWAY 時可作為目前韓國指定活動的主要綁定卡。
- 悠遊付明確標示：韓國 TWQR 不使用信用卡扣款；僅限悠遊付錢包或連結銀行帳戶。信用卡也不能用來儲值悠遊付錢包。
- 韓國常見通路表重整：CU、7-ELEVEN、emart24、GS25、EDIYA、A Twosome Place、DAISO、樂天免稅店等改成 LINE Pay／悠遊付優先判斷；OLIVE YOUNG、樂天 Mart／百貨另標示依現場與信用卡活動判斷。
- 保留 Beta 2.11.4 的 85 個景點與 Naver HAR 第四版同步結果。
- Supabase place-photos 未修改。

---

# Beta 2.11.4 — 2026-09-08

- Naver Map HAR 第四版成功抓到「釜山」共享收藏資料：84 筆 bookmark。
- 與既有資料比對：80 筆 Naver 景點已存在、0 筆被刪除、4 筆為新收藏。
- 新增：貝卡24 西面店、廣安里海景第一排咖啡廳/酒吧 海景正中央、Dondok 동독、釜山西面站抹茶芒果冰!。
- 保留既有 1 筆手動住宿「黃老師's海景民宿」，因此 APP 景點總數由 81 → 85。
- 既有景點名稱不被本次 HAR 的介面語系翻譯覆蓋，避免韓文原名被簡體中文自動翻譯取代。
- Supabase place-photos 未修改。

---

# Beta 2.11.3 — 2026-09-08

- 新增「無腦刷卡」決策：uniopen 前段 → eco 後段；只帶一張則 eco。
- 依使用者現有 LINE Pay / 悠遊付 / iPASS MONEY / 街口支付重新整理韓國支付建議。
- 新增 CU、7-ELEVEN、GS25、emart24、DAISO、Olive Young、樂天等常見店家快速支付表。
- LINE Pay + DAWAY 指定吃喝店最高 26.5% 與旅遊購物最高 9.5% 依 2026 Q3 活動更新。
- 悠遊付 9/1–12/31 韓國 TWQR 10.5% 回饋＋免 1.5% 海外交易手續費資訊更新。
- iPASS MONEY 標註目前不支援韓國跨境購物；街口支付不列為韓國主力。
- Olive Young 更新為：9/5 LINE Pay 20% 活動已結束；Unicard Mastercard 達門檻可用 Mastercard 海外回饋。
- Naver HAR 第三版經檢查只有 74 筆字型資源，沒有景點 API / Place detail，因此不修改現有 81 筆 POI。
- Supabase place-photos 無變更。

# Beta 2.11.2 — 2026-09-08

- 將刷卡指南改為使用者本人 9 張卡的韓國旅遊策略。
- 新增 uniopen 國外實體最高 11%、DAWAY 韓國 LINE Pay 指定店、富邦 J 9/20 登錄、星展 eco 5% 的實際刷卡順序。
- 補入 CUBE、Richart、Unicard、幣倍、吉鶴的條件式定位與不建議情境。
- 新增出發前卡片設定清單；過期的 DAWAY Olive Young 20% 不再視為本次旅程可用優惠。
- Supabase 後端未變更。

# Beta 2.11.1 — 2026-09-08

- Today Day 可手動切換並保存。
- 景點「探索 / 已收藏」流程修整，手機篩選改緊湊收合。
- Naver 分享網址解析退出一般新增流程，保留 Naver 查看/導航。
- FNC 五團主題色修正；AxMxP 青色標示為暫定。
- 出國注意事項由 13 個主頁籤精簡成 6 個。
- 更新 K-ETA / e-Arrival / Q-CODE / 行動電源 / 金海機場巴士 / 退稅重點。
- 新增星展 eco、富邦 J、台新 Richart、國泰 CUBE 韓國信用卡直刷回饋整理。
- 行動支付文案降噪，加入 KRW / DCC 結帳提醒。
- 全站重要副標題、次資訊與出國資訊文字放大。
- 移除金海機場退稅「Gate 4」硬編位置。

# Beta 2.11.0 — UX Rebuild（2026-09-08）

**定位：砍介面、不砍功能；把 App 重整成「首頁／景點／行程／更多」四個主要入口。**

- 首頁改成今日行程控制台，不再用九宮格堆功能。
- 「探索景點／我的景點」整合為單一「景點」工作區；搜尋、收藏、加入 Day 在同一條流程完成。
- 桌機改為 Day 左欄＋地圖中央＋景點右欄三欄工作區。
- 手機「景點」改為地圖＋可展開 Bottom Sheet，底部導覽只保留首頁／景點／行程／更多。
- 景點詳情維持地圖上的資訊卡，不再把查看景點做成多層跳頁。
- 行程的「＋新增景點」改為直接回景點搜尋，並帶入目標 Day；搜尋結果可一鍵加入該 Day。
- 「想去」改成景點狀態／收藏篩選，不再當獨立主功能。
- 分帳、分享、出國資訊、支付加值、Naver 手動新增、主題與教學統一收到「更多」。
- Naver 連結／手動座標保留為進階功能，不再佔據主要搜尋流程。
- Kakao／Google Places／OpenStreetMap 備援邏輯保留，但主介面不要求使用者理解資料來源。

# Beta 2.10.8.2 — 2026-09-07

- 「搜尋新地點」正式整合進「我的景點」，不再跳第二層搜尋視窗。
- 我的景點上方可直接用 Kakao → Google Places → OpenStreetMap 搜尋，結果可一鍵收藏或加入今天 Day。
- Naver Map 連結／手動欄位改成可收合的進階區塊，降低畫面負擔。
- 移除 AI／文字行程匯入的所有使用者入口；避免解析後還要再搜尋一次的雙層流程。
- 首頁 AI 卡改成「想去清單」，維持桌機 3×3 對齊。
- 主要頁面、工具視窗、行程、景點資訊與搜尋預覽新增「← 回上一頁」。

# Beta 2.10.8.1 — 2026-09-07

- 修正 Beta 2.10.8 資料包 `city.json` 版本仍寫成 2.10.7，現在頁首會正確顯示 Beta 2.10.8.1。
- 桌機介面整體字級放大：工具列、探索側欄、搜尋、篩選、景點清單與景點資訊卡都提高可讀性。
- 首頁改成完整 3 × 3 功能節奏，所有功能卡同寬同高，不再出現單獨一張卡、大片留白或上下大小不一。
- AI／文字行程匯入升級為桌機一級入口：首頁獨立功能卡、上方工具列「✨ AI 行程」、我的行程內皆可直接開啟。
- 「目前位置」移到 Today 卡右側操作，不再占用首頁一格，讓首頁保持整齊 3 × 3。
- GitHub 部署包改提供根目錄版，避免解壓後多一層資料夾造成上傳錯層。

# Beta 2.10.8 — 2026-09-07

- 正式介面移除 GitHub／HAR／lazy-load／圖片路徑等開發部署說明；維運資訊只留在文件。
- 首頁主要功能改成桌機 3 欄卡片節奏：我的行程／探索景點／我的景點，以及分帳／分享／出國注意事項；下方快捷工具也改為 3 欄。
- 探索篩選改為收合式面板：區域、類別、行程與更多條件分開展開；區域與類別採 3 欄排列，避免滿版 chip 造成視覺負擔。
- 搜尋改為真正的單一入口自動備援：Kakao Browser → Kakao Backend → Google Places → OSM；前一層結果不足時自動接下一層，不再停在 Kakao 失敗畫面。
- 搜尋結果加入來源／精準程度標示與重複地點合併；搜尋狀態會顯示目前哪些來源成功。
- Day 行程加入 Funliday 類型的拖曳排序、上下微調、依時間排序與「順路排序」；順路排序固定第一站，以景點間距離重新安排後續順序。
- Day 地圖新增行程折線；Kakao 與 OSM 地圖都會顯示當天景點順序的路線視覺。
- 新增「匯入文字行程」：可貼 ChatGPT／Gemini／自行整理的 Day 行程文字，解析 Day 與時間並比對現有景點，確認後快速加入行程。
- 新增 Day 路線摘要，顯示景點數與直線距離合計，清楚標註此數值僅供順路比較、不是實際交通時間。
- 隱藏一般使用者不需要的 Google Places 後端設定入口；後端功能保留，維運時仍可由程式使用。

# Beta 2.10.7 — 2026-09-07

- 移除主工具列的 1～8 區域頁籤；保留「全釜山」地圖，區域改為可複選的進階篩選。
- 地圖新增「景點／機場／換錢所／T-money／WOWPASS」快速篩選；工具地點可加入我的景點或今天 Day。
- 「探索景點」與「我的景點」共用同一條 Kakao 瀏覽器 → Kakao 後端 → Google Places → OSM 自動備援流程。
- Naver 分享頁若被擋讀或沒有座標，不再停在未定位；改用可讀名稱進入統一搜尋並顯示多筆候選供確認。
- Naver 座標解析只接受韓國合理範圍，避免把 `c=` 的縮放／畫面參數誤當成座標。
- 首頁統計、主要功能、分帳／分享與快速工具按鈕統一為同寬網格；手機改為單欄一致寬度。
- 機場注意事項補上出發機場完整流程、登機證 GATE／BOARDING TIME／SEAT、託運 X 光確認、抵韓五步驟與可勾選清單。
- 校正圖片中的液體與 Q-CODE 說法：液體採 100 ml／1 L 透明袋規則；Q-CODE 是否需要依當期檢疫條件，不宣稱所有旅客絕對必填。

# Beta 2.10.6 — 2026-09-03

- 修正 Kakao Places 瀏覽器搜尋誤呼叫不存在的 `loadKakaoSdk()`；統一使用 `loadKakaoMapSdk()`。
- 常見英文別名可補成韓文搜尋詞；例如 `lotte` 會一併搜尋 `롯데백화점 부산본점`。
- 支付・加值加入預載資料：Visit Busan 官方換錢所、釜山主要 T-money 交通節點候選、WOWPASS 金海機場官方領取方式。
- 「附近順遊」改用獨立紅色聚焦 Marker；N08 等景點不再受目前 Day／分類篩選影響。
- 分帳擴充為 25 種常用幣別；每筆消費依自身幣別取得對本位幣別匯率，USD 不再沿用 KRW 匯率。
- 消費明細與結算改為明確的 `外幣 ≒ NT$ 台幣` 顯示。
- 外觀新增 CNBLUE、FTISLAND、N.Flying、AxMxP、Hi-Fi Un!corn 五團風格主題；保留自訂主色／背景，並註明不是官方 CI 色。
- Day 行程 PNG 的地圖來源改為 Kakao Static Map；正式使用 `lv`、`logo_pos=BOTTOM_RIGHT`，保留 Kakao Logo，失敗時才使用 OSM 備援。
- 2.10.6 修正全部併入主 App IIFE 作用域，可直接存取既有 render／state 函式。

# Beta 2.10.5 — 2026-09-03
- 機場工具正式更名為「出國注意事項及刷卡指南｜金海機場 PUS」，與首頁／教學命名一致。

- 景點搜尋：Kakao JS SDK 直接搜尋為第一路徑；REST、Google 多候選、OSM 作備援。
- 搜尋紅點可再次點擊，叫回地圖預覽／加入我的景點／加入 Day。
- 換錢所、T-money、WOWPASS 加入多層 fallback，避免 API 單點失敗造成空清單。
- 分帳儲存改成不可變資料寫入＋存檔驗證，修正儲存後資料消失。
- Day 分享 PNG 次要文字放大並維持防重疊。
- 機場攻略擴充為「出國注意事項・刷卡指南」：行李清單、e-Arrival、退稅、2026/09 行動支付回饋整理。
- 預設改為 CNBLUE Blue，新增主題預設與自訂主色／背景。
- 使用教學同步更新。

# 歷代版本更新紀錄

## Beta 2.10.4｜Mobile Shell・Kakao Map・Kakao Search・支付加值（2026-08-31）

**Final Re-check（2026-09-03）**
- 修正手機行程視窗開啟時切換「首頁／探索／新增／更多」可能被舊視窗遮住。
- Kakao Map 新增「我的位置」定位標記，並支援點地圖空白處收起景點資訊。
- 分帳舊帳新增 home currency 基準，切換「我的幣別」時會先換算歷史帳務；匯率取得失敗時取消變更，避免舊帳被錯誤標成新幣別。
- 編輯歷史消費時保留原幣別與已保存匯率，不再因旅行幣別改動而誤算。
- 完整包移除 QA 暫存 JavaScript 產物。


- 發版前修正搜尋框 Enter 事件衝突：Enter 只會執行 Kakao 搜尋，不再同時觸發 OSM。
- Kakao 搜尋結果新增標準分類轉換，避免韓文細分類直接污染 App 的「餐廳／咖啡廳／購物／旅遊景點」篩選。
- Kakao Marker 移除重複座標偏移，並套用原 Smart Label 規則：縮遠以圓點為主，Day／想去／已去優先，靠近或選取時才顯示較完整標籤。
- 修正貨幣下拉狀態同步：改選 JPY／USD 等幣別後可直接更新正確匯率，交換幣別也會同步底層狀態。
- Kakao 搜尋與 Google Places 後端解耦；Google Key 暫時不可用時，Kakao 搜尋與機場官方檢查不會一起失效。
- Kakao 20 km 半徑不足時新增城市級第二輪搜尋，降低機場／外圍區域搜尋不到的情況。
- 修正手機 Bottom Navigation：首頁／探索／＋／行程／更多改為獨立事件與獨立 App Sheet，排除按鈕看得到但面板藏在 Dashboard 後方的 z-index 問題。
- 分享旅行新增手機「匯出／傳送／接收」三分頁，Day PNG、分享碼傳送與分享碼接收不再藏在長頁面底部。
- 接入 Kakao Map Web SDK；韓國地圖預設使用 Kakao，可切回 OSM。
- 景點直接搜尋改為 Kakao Place Search 優先，Google Places、OSM 作備援；結果可直接加入我的景點或目前 Day。
- 分帳旅行幣別／本位幣別改成常用貨幣下拉選單，新增交換幣別。
- 「換錢所」擴充為「支付・加值」：換錢所、T-money、WOWPASS 三分頁。
- Naver Map 自訂景點解析合併 2.10.3.1 的定位修正，但 2.10.3.1 版本號本身維持作廢。
- 使用教學同步更新。
- Supabase Edge Function 新增 Kakao REST 搜尋；需新增 Secret `KAKAO_REST_API_KEY`。

## Beta 2.10.3｜App Shell・直接搜尋景點（2026-08-25）

- 手機新增固定 Bottom Navigation：首頁、探索、新增、行程、更多。
- 首頁新增「目前 Day」卡片，可直接查看當天行程。
- 新增單欄位「搜尋新地點」，預設使用 OpenStreetMap Nominatim。
- Nominatim 搜尋只在按下搜尋時呼叫、不做 autocomplete，並加入 7 天本機查詢快取。
- 搜尋結果可直接地圖預覽、加入我的景點、加入今天 Day。
- 加入可選的「Google 精準搜尋」，沿用既有 Supabase + Google Places 後端。
- 手機景點詳情改成 Bottom Sheet 視覺。
- 記憶最後地圖中心與 Zoom，返回 App 時維持操作位置。
- 我的景點「地圖」改為「📍 地圖定位」，有效位置使用 flyTo 動畫，無效位置會明確提示。
- 使用教學同步加入 Bottom Navigation 與直接搜尋新地點流程。

## Beta 2.10.2｜Day 圖文字排版・機場官方更新檢查（2026-08-24）

- 修正 Day 行程圖長景點名稱與備註重疊問題。
- Day PNG 先計算標題實際行數，再配置備註位置；空間不足時寧可省略次要文字，不再互相覆蓋。
- 機場攻略新增「檢查最新資訊」。
- 超過 7 天後第一次開啟機場攻略會重新檢查 KAC／釜山市官方來源。
- 官方來源內容有變化時顯示提醒，但不自動臆測或重寫規定。
- 使用教學同步加入機場資訊更新流程。

## Beta 2.10.1｜實機回饋修正版

- 修正旅行分帳新增旅伴。
- 修正 Naver Map 短網址座標解析與 `0,0` 誤存。
- 我的景點資訊卡新增直接編輯按鈕。
- Day 行程圖改為真實 OSM 地圖圖層＋路線與順序標記。
- 分享旅行拆分傳送方／接收方。
- 換錢所新增完整地址與中文釜山行政區。
- 使用教學更新為完整手把手流程。
- 發版規則：後續每版都同步更新使用教學。

## Beta 2.10 — 旅行分帳・匯率・分享

- 新增旅行分帳：旅伴、消費、付款人、平均／自訂分帳、最終結算。
- 新增旅行幣別／本國幣別與手動匯率。
- 新增網路參考匯率更新，並保存每筆消費當下匯率。
- 新增 1920×1080 Day 行程 PNG（行程清單＋地圖＋編號順序）。
- 新增圖片 Share Sheet。
- 新增 `TRIPMAP1` 旅行分享碼、複製、系統分享、合併／覆蓋匯入。
- 分帳與分享資料延續 Local First，不新增登入需求。

## Beta 2.9.3｜Google Places 換錢所・介面動態效果（2026-08-24）
- 正式以 Beta 2.9.2 為基準延伸；先前測試中的 NAVER API HUB 方案不列入正式版本。
- 換錢所主資料來源改為既有的 Google Places API，不再需要申請 NAVER Cloud / NAVER API HUB。
- Google Places 會即時搜尋釜山換錢所，回傳名稱、地址、座標、類型與 Google 營業狀態；永久停業項目會排除。
- 換錢所資料採 6 小時本機快取以降低 API 呼叫；「立即更新」可強制重新讀取。
- Google Places 無法使用時，依序切換 OSM 備援、最近快取、內建備援清單。
- 每間換錢所保留「Naver 查看」，以名稱＋地址開啟 NAVER Map 搜尋，供韓國在地資訊與導航確認。
- 「我的景點」仍可貼 Naver Map 分享連結；後端會解析可取得的線索，再用 Google Places 配對名稱、地址、座標與分類，不要求使用者手填經緯度。
- 新增 Dashboard 色彩層次、卡片互動、主要 Modal 淡入／上浮動畫與手機 Bottom Sheet 滑入動畫。
- 動畫時間約 0.25～0.35 秒，並支援系統「減少動態效果」設定。

> 這份檔案可直接放在 GitHub 專案根目錄。後續每個版本都往最上方追加。

## Beta 2.9.2｜Dashboard 排版・參考 URL・換錢所即時同步（2026-08-24）
- Dashboard 重新排版：三個主要功能卡等寬對齊，移除重複的「探索景點」入口。
- 機場攻略、換錢所、目前位置、使用教學改為同一組整齊的快捷卡，不再獨立散落在最下方。
- 首頁切換到地圖加入約 0.35 秒淡出過場。
- 「我的景點」重新加入獨立的「參考 URL」欄位，可保存 Threads、Instagram、官方網站、部落格等來源；Naver Map 連結改為獨立欄位。
- 舊自訂景點若曾把 Naver URL 存在參考 URL，編輯時會自動辨識並移到 Naver Map 欄位。
- 換錢所開啟時自動向 OpenStreetMap / Overpass 重新同步目前登錄的換錢所，不再預設只讀 7 天快取。
- OSM 有提供時，同步顯示地址、營業時間、電話與網站資訊。
- 換錢所卡片本身可直接點擊；點選後會關閉清單與首頁，直接切回地圖並定位、開啟標記。
- 若 OSM 即時同步失敗，才改用最近快取；沒有快取時再顯示備援清單。

> 這份檔案可直接放在 GitHub 專案根目錄。後續每個版本都往最上方追加。

## Beta 2.9.1｜介面修正版・Naver 快速新增（2026-08-24）
- 首頁統計卡改為可直接開啟對應內容。
- 景點地圖與找景點整合為「探索景點」。
- 移除固定「81 個景點」文案，數量由目前資料動態計算。
- 我的景點支援貼上 Naver Map 地點連結自動讀取。
- 自動取得位置與地址，分類／區域預設自動判斷並保留手動選項。
- 經緯度移到進階設定，不再要求一般使用者手動填寫。
- 清除首頁與功能畫面中的開發備註與版本預告文字。

> 這份檔案可直接放在 GitHub 專案根目錄。後續每個版本都往最上方追加。

## Beta 2.9 — App Dashboard・Smart Labels（2026-08-24）

**定位：把「地圖工具」往真正旅行 App 的操作方式推進。**

- 新增首頁 Dashboard，使用者先選要做的事，不再直接進入 81 點滿版地圖。
- Dashboard 提供：我的行程、景點地圖、找景點、我的景點；分帳與分享先顯示 Beta 2.10 預告。
- Dashboard 顯示景點總數、已排行程、想去、已去摘要。
- 景點標籤新增 `自動／全部／關閉` 三種模式；預設自動。
- 自動模式依縮放層級逐步顯示標籤，縮小時只留圖釘，降低畫面雜訊。
- 點選中的景點保留高亮與名稱。
- 景點詳情、結果清單、Day 行程、編輯表單、按鈕等文字全面放大。
- 頂端工具列精簡，次要工具移至 Dashboard／更多。
- 使用教學不再每次首次載入時自動蓋住畫面，改為需要時開啟。
- 完整保留 Beta 2.8 的 Local First、IndexedDB、自訂景點與 Google Places / Supabase Proxy。

## Beta 2.8 — 公版核心・Local First（2026-08-22）

**定位：把釜山單一版本拆成可延伸其他城市的資料架構，並把個人資料留在本機。**

- 景點主資料從 `index.html` 拆到 `data/poi-data.json`。
- 城市、地圖中心、區域、顏色、View、Day 數量等搬到 `data/city.json`。
- 81 個釜山景點完整保留。
- 個人 Day、想去／已去、行程備註等改為 IndexedDB 優先保存，並保留 localStorage 相容鏡像。
- 可自動讀取舊版 localStorage 旅程資料。
- 新增「我的景點」：使用者可在前台新增／修改／刪除只存在本機的景點。
- 自訂景點可加入 Day、設定狀態、使用 Google Places 實景照片。
- 保留 JSON 備份／匯入作為技術備援。

## Beta 2.7 — Supabase Places Proxy（2026-08-21）

**定位：把 Google API Key 從瀏覽器移到後端。**

- 移除前端手動貼 Google API Key 的方式。
- 新增 Supabase Edge Function `place-photos`。
- Google Places API Key 改放 Supabase Secret `GOOGLE_MAPS_API_KEY`。
- GitHub Pages 前端只保存 Supabase Project URL / Publishable Key。
- Google Places 改走 Supabase Proxy 取得地點與實景照片。
- 保留 Place ID 快取，避免每次重新做 Text Search。
- 實景照片補上 Google / 作者 attribution 與來源地點連結。

## Beta 2.6 — Google Places 實景照片

**定位：第一次把 Google Places 店景／料理實景照片接進景點卡。**

- 景點卡加入 Google Places 實景照片。
- 可取得餐廳／咖啡廳／景點的店景與料理照片。
- 當時 Google API Key 仍由瀏覽器端設定，因此後續 Beta 2.7 改成 Supabase Secret 架構。

## Beta 2.5 — 81 景點地圖基準版

**定位：後續版本的主要地圖與旅遊功能基礎。**

- 建立釜山 81 景點地圖與 1～8 區域 View。
- 景點標籤、分類與區域配色。
- 景點搜尋及類別／狀態／Day／距離等交叉篩選。
- 我的行程、旅行狀態、地圖定位與景點資訊卡。
- 金海機場攻略、換錢所、旅行模式等旅行工具。
- 地圖 PNG 匯出與基本備份／匯入流程。

---

## 接下來

- **Beta 2.10**：分帳＋匯率＋Day 行程 PNG＋旅行分享碼＋手機 Share Sheet。
- **Beta 2.11**：Travel Map Manager，讓 NAVER HAR／Excel／CSV 更新景點與 GitHub 部署包自動化。
- **Beta 3.0**：PWA，加入主畫面、App icon、全螢幕與離線 App shell。

## Beta 2.10.2｜2026-08-24
- 修正 Day 行程 PNG：標題換行時，備註／交通文字不再與標題重疊；依實際標題行數動態排版，空間不足時自動省略次要文字。
- 機場攻略新增「官方資料檢查」：每 7 天開啟機場攻略時自動檢查一次，也可手動按「檢查最新資訊」。
- 檢查來源包含 KAC 金海機場首頁、設施、聯絡、航班資訊與釜山市交通來源；若與本機上次檢查的頁面指紋不同，會提示官方頁面已有變更。
- 使用教學同步更新機場攻略的自動／手動檢查方式。
- 本版機場功能屬「官方來源變更偵測」，不會自動臆測或重寫政策、退稅、安檢等複雜規則。

## Beta 2.11.15 — 2026-09-08
- 全域一致性 QA，不只檢查本次 More 修正。
- 清除／隱藏正式版殘留的開發設定文案與照片後端測試介面。
- 手機底部導覽原始 HTML 統一為四項，避免載入前舊五鍵閃現。
- 修正搜尋／手動新增／教學的過時 Naver 與技術文案。
- 移除會干擾 More 動態定位的舊 CSS top 規則。

## Beta 2.11.16 — 2026-09-08
- 修正手機景點搜尋欄因虛擬鍵盤 resize 觸發 DOM 重掛載而失焦／鍵盤自動收起。
- `mountPlaces211()` 改為 idempotent：目標父節點相同時不再 append。
- resize 僅在跨越 900px 桌機／手機 breakpoint 時重新掛載搜尋 DOM。
- 搜尋 input 補強手機鍵盤與輸入設定。
