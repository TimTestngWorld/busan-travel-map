# Beta 2.12 Final｜Day 行程地圖預覽 Hotfix（2026-09-10）

## 本次新增 / 修正
- 新增 **「預覽本日行程地圖」** 按鈕，可在不匯出 PNG 的情況下，直接預覽當日行程地圖。
- 新增互動式 Day 地圖預覽視窗：支援 **拖曳、放大、縮小、回到全部景點範圍**。
- 匯出 Day 行程圖時，地圖會依景點範圍重新計算較合適的視角，並加入 **近距離標記避讓**，降低多個景點黏在一起的情況。
- 預覽視窗左側會同步顯示當日景點清單，點擊景點可直接在地圖上定位。
- 已重新執行全域回歸檢查，確認搜尋、地圖、Day、分享、備份、住宿、出國、換錢與支付、分帳、PWA 與資料保存流程沒有因本次修改而退步。

---

# 釜山旅行地圖 CHANGELOG

## Beta 2.12 Final｜2026-09-10｜封版候選

本版以 Beta 2.12.13 Rev C 為基準，不新增大型功能；重點是完整全域回歸、資料安全、PWA 更新與跨頁一致性。

- 完成 97 項全域回歸檢查：桌機／手機、地圖、搜尋、Day、收藏、住宿、出國、換錢與儲值、刷卡與支付、分帳、分享、備份、PWA、版本與資料完整性均通過靜態／結構驗證。
- 修正 localStorage／IndexedDB 雙儲存的「最後一次操作可能被較舊 IDB 蓋回」風險；新增 `_savedAt`，啟動時選擇較新的狀態並回填兩邊。
- 手機「更多」的備份／還原改成直接呼叫正式功能，不再繞到桌機專用備份區。
- 匯入備份新增城市 ID 檢查；若是其他城市／公版資料集，會先要求確認，避免誤覆蓋目前旅程。
- 所有加入行程入口再次全域稽核為 `＋ Day X`；legacy 路徑不再靜默塞入「今天 Day」。
- 清除搜尋、20 → 40 → 60、搜尋照片 Lazy Load、地圖編號、附近排序、搜尋詳情讓位與 Bottom Sheet 拖曳均保留。
- PWA 改為 versioned offline shell：HTML 上線時永遠 network-first / no-store，避免舊 Service Worker 鎖版；離線時可開核心 App、85 景點資料與本機旅程狀態。地圖圖磚、線上搜尋、Google Places 照片、天氣／匯率等即時資料仍需網路。
- 金海機場官方資訊重新核對至 2026-09-10；韓文 KAC 頁的國際線機場警察更新為 `051-899-3279`，並保留英／中頁仍刊舊號碼的差異提醒。
- 機場巴士與信用卡／支付活動重新核對至 2026-09-10；沒有發現需要更改既有核心回饋數字的項目。
- 快速教學、部署說明、版本標示與 PWA cache-bust 全部統一為 Beta 2.12 Final。

## Beta 2.12.13 Rev C｜2026-09-10｜完整稽核版

- 所有加入行程入口跨頁統一為 `＋ Day X`。
- 搜尋新增明確「清除搜尋・回景點」。
- 修正換錢／T-money／WOWPASS「搜尋我附近」事件。
- 手機「更多」新增資料備份／還原入口。
- reset-pwa 僅處理本 App scope；補 Leaflet CSS 與 OSM fallback。

## Beta 2.12.12｜2026-09-10｜Mobile Search / Detail UX

- 點選搜尋結果或既有景點後，第一層搜尋 Bottom Sheet 自動讓位。
- 詳情關閉後恢復搜尋面板；大量搜尋結果取消會遮住清單的 sticky 控制列。

## Beta 2.12.11｜2026-09-10｜Bottom Sheet Root Cause

- Bottom Sheet／PWA 初始化移回主 async App 初始化流程，排除 DOM 尚未建立就綁事件的時序與 scope 問題。

## Beta 2.12.0｜2026-09-09｜2.12 主線開始

- 線上搜尋由固定 20 筆改為 20 → 40 → 60 分批顯示；附近排序會針對完整候選池排序。
